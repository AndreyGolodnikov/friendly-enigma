Создал пустую конфигурацию на управляемых формах с возможностью загрузки данных из файла json.

Добавил справочник Номенклатура + реквизиты к нему и табличную часть

Реквизиты: Описание и Артикул:

Описание - строка неограниченной длины - (в файле поле "text")

Производитель - строка 100 - (в файле поле "vendor")

Артикул - строка 50 - (в файле, например, "V3230")

В табличной части реквизиты Код, Размер, Цена.

1.Сделал обработку по заполнению справочника Номенклатура из этого файла.

2.Файл выбирается через диалог выбора файла, а не через прямой путь.

3.Дубли не создаются, товар сопоставляется с теми что в базе и с теми что в файле по Артикулу.

4.Если товара нет в базе то создаем его и заполняем ВСЕ реквизиты, если уже есть то обновляем ТОЛЬКО описание к нему (поле "text").

5.Проверку наличия номенклатуры в базе производится запросом.

6.Проверка изменения описания номенклатуры (в файле поле "text") производить запросом.
